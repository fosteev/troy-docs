# {Name EN} — {Название RU}

> Шаблон карточки класса. Скопировать в `classes/<code>.md`, заполнить все `{…}`, удалить блоки «💡». Эталон заполнения — [warrior.md](warrior.md).
> Кап уровня — **10**, скиллов — **4** (слоты 1 / 3 / 6 / 10). Числа отсюда идут в seed/админку и в `troy-assets/assets/classes/<code>.yaml`, не наоборот.

## 1. Название

| | |
|---|---|
| `code` | `{code}` — латиница, snake_case, стабильный id для клиента, не меняется никогда |
| Название (RU / EN) | {Название} / {Name} |
| Дев-двойник | {нет | `<code>_dev`, если арт пробуем на копии} |

## 2. Описание

**Роль:** {дистанция (мили/дальний) · функция (танк/дамагер/контроль) · темп (разгон/фронтлоад)} — одна строка.

**Лор (экран выбора, ≤ 220 символов):**
> {2–3 предложения от третьего лица: кто, во что верит, как ведёт бой. Без чисел.}

**Короткое (карточка, ≤ 60 символов):** «{…}».

**Тэги стиля игры:** {танк · мили · разгон · простой} — 3–4 тэга.

**Сильные стороны:** {3 пункта}.
**Слабые стороны:** {3 пункта — обязательно, класс без слабостей не балансируется}.

💡 Ресурсная идентичность обязана читаться в описании: RAGE-класс «разгоняется», MANA-класс «сильный старт, потом экономит».

## 3. Основной ресурс — {Rage | Mana}

| | |
|---|---|
| `resourceType` | `{RAGE | MANA}` |
| Диапазон | {RAGE: 0–100 фикс. | MANA: 0–(baseMana + INT×8)} |
| Старт боя | {RAGE: 0 | MANA: полный} |
| Вне боя | не существует |
| UI | {цвет полосы под HP} |

```
{RAGE:  rage += 5 * (1 + SPI*0.02) за автоатаку; += damage_taken * 0.15 * (1 + SPI*0.02) за урон}
{MANA:  regen = max_mana * 0.01 * (1 + SPI*0.02) в секунду}
```

💡 Новый тип ресурса (Energy, Combo…) = новая формула в `game-core/battle` и enum `ResourceType` — отдельная задача, здесь только описать.

## 4. Характеристики

### Стартовые (уровень 1) и авто-рост

| Атрибут | Старт | За уровень | На 10 ур. |
|---|---|---|---|
| Strength | {} | {} | {старт + рост×9} |
| Intelligence | {} | {} | {} |
| Stamina | {} | {} | {} |
| Agility | {} | {} | {} |
| Spirit | {} | {} | {} |

💡 Бюджет (`content-generation.md`): старт — сумма **22**, основной стат 8, второй 5–6, остальные 2–4; рост — сумма ровно **6.5**, основной +2.5, второй +1.5–2.0. Плюс 2 свободных очка/уровень (18 за 2→10).

Константы (`CharacterClass`): `baseHp {17 стекло … 22 танк}`, `baseMana {0}`, `baseAttackSpeed {0.5 медленный кастер … 0.8 быстрый мили}`.

### Производные

```
HP = baseHp + STA*10 + STR*2      Armor = STA*2      MR = INT*1.5
Attack Speed = baseAS * (1 + AGI*0.01)      Crit = AGI*0.15 %
Автоатака = {base_weapon_dmg + STR*0.8 (PHYSICAL) | base_spell_dmg + INT*0.5 (MAGICAL)}
```

### Таблица 1–10

💡 Посчитать скриптом, не руками (см. `python`-блок в конце файла), вставить сюда.

| Ур. | STR | INT | STA | AGI | SPI | HP | Armor | MR | Atk speed (интервал) | Crit | {Rage/auto | Mana / regen} |
|---|---|---|---|---|---|---|---|---|---|---|---|
| … |

### Билды (18 свободных очков)

| Билд | Куда очки | Что даёт на 10 ур. |
|---|---|---|
| {4 билда — по одному на стат, который имеет смысл} | | |

### Экипировка

{Оружие (определяет визуал автоатаки и скиллов!), тип брони, что запрещено.}

## 5. Скиллы

Слоты **1 / 3 / 6 / 10**. Урон = `baseDamage + {STR|INT} * scalingRatio`.

| Слот | Ур. | code | Название EN / RU | Cast | CD | Cost | Base | Scaling | Эффект | Статус |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1 | `{}` | | | | | | | | план |
| 2 | 3 | `{}` | | | | | | | | план |
| 3 | 6 | `{}` | | | | | | | | план |
| 4 | 10 | `{}` | | | | | | | | план |

💡 Бюджет слотов: 1 — хлебный нюк, CD 3–4s, cost 15–25, base 20–35, ×1.0–1.2; 2 — контроль STUN/SLOW, CD 5–10s, cost 20–35, base 10–18, ×0.5–0.8; 3 — утилити BUFF/ABSORB/HEAL, CD 18–20s, cost ~25, base 0; 4 — тяжёлый нюк, каст 1–2.5s, CD 12–15s, cost ~40, base 40–80, ×1.2–1.8. Cost для RAGE — очки из 100, для MANA — под ману уровня анлока ((INT на уровне)×8). `effectType` ∈ NONE STUN SLOW DOT BUFF ABSORB HEAL.

Экономика ресурса на 10 ур.: {сколько ресурса в секунду → как часто каждый скилл}.

Поля `ClassSkill`:

```yaml
{code}: { slot: 1, unlockLevel: 1,  castTimeSec: 0, cooldownSec: 4, resourceType: {RAGE|MANA}, resourceCost: 25,
          damageType: {PHYSICAL|MAGICAL|null}, baseDamage: 20, scalingStat: {STR|INT|null}, scalingRatio: 1.2,
          effectType: NONE, effectValue: 0, effectDurationSec: 0 }
# … ×4
```

### Описания и промты

💡 `icon` — **предмет/символ**, не персонаж: одна вещь + один цветовой акцент эффекта (impact flash, stun stars, frost, …), читается в 32 px. `cast` — «{Name} — что делает тело, что делает оружие, какой VFX», глаголы действия, ≤ 30 слов, без ракурса и фона (их добавит шаблон `styles/class.yaml`: «…, facing right, clear wind-up then the action with follow-through»).

**{Skill 1}** — «{одна фраза для тултипа}»
- `icon`: `{…}`
- `cast`: `{Name} — {…}`
- ⬜ не сгенерировано

**{Skill 2}** … ×4

## 6. Максимальный уровень

**10.** XP `80 * level^1.5` — таблица общая, см. `leveling.md`. На 10 ур.: 4 скилла, 18 очков, кап.

## 7. Арт: промты на каждую картинку

### Визуальный бриф

- Силуэт: {3–4 признака, из них **один опознавательный** — читается в 32 px и сверху}.
- Цвета: {2 базовых + золото только как акцент}. Палитра общая: `palette/troy-dark-gold.png`.
- Размеры фиксированы: персонаж 128 (холст 144), вид сверху 64 (холст 80), иконка 128→×4 = 512, портрет 128.

### `subject`

```
{Пол/раса и телосложение}, {броня/одежда с материалами и отделкой},
{оружие в правой руке}, {что в левой руке / за спиной},
{головной убор + опознавательный признак}, {обувь}.
Color scheme: {цвет 1}, {цвет 2}, warm gold accents, muted dark medieval fantasy.
```

💡 Только внешность. Без ракурса, позы, фона, «pixel art», «transparent» — это добавляют шаблоны.

### Поля манифеста (`troy-assets/assets/classes/{code}.yaml`)

| Поле | Значение | Куда подставляется |
|---|---|---|
| `emblem` | {символ класса: оружие + один предмет + опознавательный признак} | icon 512 |
| `portrait` | {лицо крупно: раса, шлем/капюшон, шрамы, взгляд, деталь на шее} | portrait |
| `weaponRest` | {как оружие лежит в спокойной стойке} | keyframeSide, idle |
| `weaponCarry` | {что в какой руке при ходьбе} | walk |
| `topdownDetail` | {что видно сверху: головной убор, плечи, оружие по сторонам} | keyframeTop |
| `attackMotion` | {замах → удар, всем телом; для кастера — жест → выброс} | attack |

### Ключевые кадры и анимации

💡 Промты собираются шаблоном `styles/class.yaml`; после генерации сюда копируются **итоговые строки из `<code>.state.json`** (`node -e` дамп — см. warrior.md). До генерации — оставить таблицу с «→ из state».

| Слот | Style | Размер / кадры | Промт |
|---|---|---|---|
| `keyframeFront` | `rd_pro__fantasy` | 128 | → из state |
| `keyframeSide` | `rd_pro__edit` (input = front) | 128 | → из state |
| `keyframeTop` | `rd_pro__edit` (input = front) | 64 | → из state |
| `icon` → `iconUrl` | `rd_plus__skill_icon` ×4 | 512 | → из state |
| `portrait` | `portrait:rd_flux` | 128 | → из state |
| `spriteIdle` | `rd_advanced_animation__idle` | 8 / 5 fps | → из state |
| `spriteWalk` | `rd_advanced_animation__walking` (от top) | 8 / 10 fps | → из state. **Канон: идёт строго вверх (север), голова у верхнего края** |
| `spriteAttackIdle` | `custom_action` (от side) | 8 / 5 fps | → из state |
| `spriteAttack` | `custom_action` (от side) | 8 / 12 fps | → из state |
| `ClassSkill.spriteAttack` ×4 | `custom_action` (от side) | 8 / 12 fps | раздел 5 |

Требования: idle — едва заметное дыхание; attackIdle — стоит, не машет; attack — замах → удар; walk — вид сверху (GTA 2).

**Канон направления для карты (обязателен для всех классов):** `keyframeTop` и `spriteWalk` — персонаж смотрит и шагает **строго вверх (север)**: голова у верхнего края холста, ноги у нижнего, корпус не повёрнут и не наклонён. Один лист на все направления — клиент поворачивает маркер на угол heading (0° = север, по часовой). Приёмка: положить лист рядом с `knight`/`mage` — головы у всех наверху; лист, где персонаж идёт вбок/вниз или «в камеру», — брак, перегенерировать. Ходьбу с проездом по карте синхронизируем позже (fps ↔ скорость), поэтому у всех классов walk — 8 кадров, 10 fps, один полный цикл шага.

### Чек-лист готовности арта

- [ ] keyframeFront, keyframeSide, keyframeTop
- [ ] icon 512, portrait
- [ ] spriteIdle, spriteWalk, spriteAttackIdle, spriteAttack
- [ ] 4 × (skill icon + cast)
- [ ] проверка на устройстве: карта, бой, выбор персонажа
- [ ] залито `publish.mjs` под `code: {code}`

## 8. Реализация

Порядок: карточка → скиллы и класс в админке (`isActive: false`) → манифест `troy-assets` → `gen.mjs` по шагам → `publish.mjs` → устройство → `isActive: true`.

### Расхождения код ↔ документы

{пусто или список}

---

```python
# Таблица 1–10: подставить старт/рост, base, ресурс — вывести и вставить в раздел 4.
S={'STR':(8,2.5),'INT':(2,.5),'STA':(6,2),'AGI':(3,1),'SPI':(3,.5)}; baseHp,baseAS,baseMana,res=22,0.8,0,'RAGE'
for L in range(1,11):
    v={k:a+b*(L-1) for k,(a,b) in S.items()}; hp=baseHp+v['STA']*10+v['STR']*2; AS=baseAS*(1+v['AGI']*.01)
    last=f"{5*(1+v['SPI']*.02):.2f}" if res=='RAGE' else f"{baseMana+v['INT']*8:g} / {(baseMana+v['INT']*8)*.01*(1+v['SPI']*.02):.2f}/с"
    print(f"| {L} | "+" | ".join(f"{v[k]:g}" for k in S)+f" | {hp:g} | {v['STA']*2:g} | {v['INT']*1.5:g} | {AS:.3f} ({1/AS:.2f}с) | {v['AGI']*.15:.2f}% | {last} |")
```
