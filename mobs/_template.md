# {Name EN} — {Название RU}

> Шаблон карточки моба. Скопировать в `mobs/<code>.md`, заполнить `{…}`, удалить «💡».
> Числа отсюда идут в seed/админку и в манифест `troy-assets/assets/mobs/<code>.yaml`.

## 1. Название

| | |
|---|---|
| `code` манифеста | `{code}` (snake_case; в БД моб ищется по `name`) |
| `name` (в БД, EN) | {Name} |
| Название RU | {Название} |
| `isElite` | {false | true — босс: спрайт 192, стиль horror} |

## 2. Описание

**Роль в бою:** {танк/дамагер/контроль · чем опасен · чем берётся} — одна строка.

**Описание — `Monster.description` (в игре: тап по маркеру, интро боя; RU, ≤ 220):**
> {1–2 предложения: кто это и как дерётся. Без чисел.}

## 3. Характеристики и награды

| | | | |
|---|---|---|---|
| level | {} | hp | {} |
| strength | {} | intelligence | {} |
| armor | {} | magicResist | {} |
| attackSpeed | {} | dodge | {} |
| expReward | {} | goldReward | {} |
| spawnable | {true} | nothingWeight | {} |

💡 Бюджеты по уровням — `game-design/content-generation.md` (таблица мобов): hp ≈ 60+18·lvl,
награды по кривой XP. Кап уровня игрока — 10, мобы 1–10 (+элитки).

## 4. Дроп

| Item | weight | minQty | maxQty |
|---|---|---|---|
| {…} | | | |

## 5. Скиллы (0–2, у элиток до 2)

Алгоритм боя моба: скиллы проверяются по `sortOrder` (меньше — раньше), `condition` гейтит
(`always` / `opener` / `self_hp_below:N` / …) — см. `game-design/combat.md`.

| sortOrder | code | name (EN) | condition | Cast | CD | Dmg type | Base | Scaling | Эффект |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `mob_{…}` | {} | {} | {} | {} | {} | {} | {} | {} |

**{Skill}** — `description` (в игре, RU): {что делает + число эффекта}
- `icon`: `{предмет/символ + один цветовой акцент}`
- `cast`: `{Name} — {тело, оружие, VFX; ≤ 30 слов}`

## 6. Арт: промты

Конвейер: `troy-assets/styles/mob.yaml` + манифест `assets/mobs/{code}.yaml`;
итоговые промты после генерации — из `{code}.state.json` сюда.

### Визуальный бриф

- Силуэт: {3–4 признака, один опознавательный — читается на 32 px}.
- Цвета: {2 базовых + один акцент}; `accentColor` для маркера — {…}.
- Размеры: моб 96–128 (босс 192), иконка-маркер 64→×2 (рисуется 32–48).

### `subject`

```
{Раса/тварь, телосложение}, {шкура/броня}, {оружие/когти}, {опознавательный признак}.
Color scheme: {…}, muted dark medieval fantasy.
```

### Поля манифеста

| Поле | Значение |
|---|---|
| `emblem` (маркер) | {голова/символ моба} |
| `accentColor` | {один доминирующий цвет маркера} |
| `weaponRest` | {как держит оружие в стойке} |
| `attackMotion` | {замах → удар} |
| `hitMotion` | {реакция на удар слева} |
| `deathMotion` | {как падает} |
| `arena` (фон арены) | {сцена логова: местность + 2–3 детали, без существ и текста} |

### Слоты (канон: в бою моб справа, смотрит ВЛЕВО; клиент не зеркалит)

| Слот БД | Style | Кадры/fps | Промт |
|---|---|---|---|
| keyframeSide (влево) | `rd_pro__fantasy` (босс: `rd_pro__horror`) | 128 | → из state |
| `iconUrl` (маркер) | `rd_plus__skill_icon` ×2 | 64→128 | → из state |
| `spriteIdle` | `rd_advanced_animation__idle` | 8 / 5 | → из state |
| `spriteAttack` | `custom_action` | 8 / 12 | → из state |
| `spriteHit` | `custom_action` | 6 / 12 | → из state |
| `spriteDeath` | `custom_action` (или `__destroy`) | 8 / 8 | → из state |
| `MonsterSkill.spriteAttack` | `custom_action` | 8 / 12 | раздел 5 |
| `arenaBackground` | `rd_pro__fantasy` 256 (opaque) | 1×1 | → из state (`arena` в манифесте) |

### Чек-лист

- [ ] маркер читается на карте (32 px, один доминирующий цвет)
- [ ] idle/attack/hit/death; hit — отдача вправо; death — лежит в последнем кадре
- [ ] скиллы (icon + cast), фон арены
- [ ] заведён в БД (админка/seed), publish залил визуал, проверка на устройстве

## 7. Реализация

Моб (статы/дроп) — админка (или seed для базовых 10); publish находит его по `name` и заливает
визуал, `description`, фон арены, создаёт недостающие скиллы из `db:`-блоков манифеста.

### Расхождения код ↔ документы

{пусто или список}
